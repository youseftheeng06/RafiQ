/**
 * Production API origin → loaded before JS/master.js
 * Paste your HTTPS Render/Railway URL here (scheme + host, no trailing slash, no '/api').
 * Examples: https://rafiq-api.onrender.com  or  https://your-app.up.railway.app
 * Leave empty to use localhost:8080 during local development.
 *
 * Overrides API_ORIGIN_OVERRIDE in master.js when set non-empty (see DEPLOYMENT.md).
 */
window.__RAFIQ_API_ORIGIN__ = "";
